sourced=0
(return 0 2>/dev/null) && sourced=1
if [ $sourced == 0 ]; then
    echo "Source this file with '. $(basename $0)', don't execute it" >&2
else
    echo "ok"
fi

. ~/.virtualenvs/tartley.com/bin/activate

